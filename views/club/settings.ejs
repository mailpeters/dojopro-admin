<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Club Settings - DojoPro Admin</title>
    <link href="/css/admin.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <!-- Header -->
    <div class="header">
        <div class="header-content">
            <div class="logo">🥋 DojoPro</div>
            <div class="club-info">
                <% if (userClub) { %>
                    <div class="club-name"><%= userClub.club_name %></div>
                    <div style="font-size: 0.9rem; opacity: 0.8;">Club Management Portal</div>
                <% } %>
            </div>
            <div class="user-info">
                <div><%= user.first_name %> <%= user.last_name %></div>
                <form method="POST" action="/logout" style="display: inline;">
                    <button type="submit" class="btn-logout">Logout</button>
                </form>
            </div>
        </div>
    </div>

    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-brand">
                <a href="/dashboard">DojoPro Admin</a>
            </div>
            <div class="nav-menu">
                <a href="/dashboard" class="nav-link">
                    <i class="fas fa-tachometer-alt"></i> Dashboard
                </a>
                <a href="/members" class="nav-link">
                    <i class="fas fa-users"></i> Members
                </a>
                <a href="/locations" class="nav-link">
                    <i class="fas fa-map-marker-alt"></i> Locations
                </a>
                <a href="/staff" class="nav-link">
                    <i class="fas fa-user-tie"></i> Staff
                </a>
                <a href="/checkins" class="nav-link">
                    <i class="fas fa-clock"></i> Check-ins
                </a>
                <a href="/settings" class="nav-link active">
                    <i class="fas fa-cog"></i> Settings
                </a>
            </div>
        </div>
    </nav>

    <div class="main-content">
        <div class="content-header">
            <h1><i class="fas fa-cog"></i> Club Settings</h1>
        </div>

        <% if (messages.success) { %>
            <div class="alert alert-success">
                <i class="fas fa-check-circle"></i> <%= messages.success %>
            </div>
        <% } %>

        <% if (messages.error) { %>
            <div class="alert alert-error">
                <i class="fas fa-exclamation-circle"></i> <%= messages.error %>
            </div>
        <% } %>

        <form method="POST" action="/settings">
            <div class="card">
                <div class="card-header">
                    <h3><i class="fas fa-info-circle"></i> Basic Information</h3>
                </div>
                <div class="card-body">
                    <div class="form-group">
                        <label for="club_name">Club Name *</label>
                        <input type="text" id="club_name" name="club_name" required 
                               value="<%= clubDetails ? clubDetails.club_name : '' %>"
                               placeholder="Enter your club name">
                        <small class="help-text">This is how your club name appears throughout the system</small>
                    </div>

                    <div class="form-group">
                        <label for="description">Description</label>
                        <textarea id="description" name="description" 
                                  placeholder="Brief description of your club, martial arts style, and what makes you unique"><%= clubDetails ? clubDetails.description || '' : '' %></textarea>
                        <small class="help-text">A brief description of your club, martial arts style, and what makes you unique</small>
                    </div>

                    <div class="form-group">
                        <label for="website_url">Website URL</label>
                        <input type="url" id="website_url" name="website_url" 
                               value="<%= clubDetails ? clubDetails.website_url || '' : '' %>"
                               placeholder="https://yourclub.com">
                        <small class="help-text">Your club's website (optional)</small>
                    </div>

                    <div class="form-group">
                        <label for="logo_url">Logo URL</label>
                        <input type="url" id="logo_url" name="logo_url" 
                               value="<%= clubSettings ? clubSettings.logo_url || '' : '' %>"
                               placeholder="https://example.com/logo.png">
                        <small class="help-text">URL to your club logo image (optional)</small>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h3><i class="fas fa-palette"></i> Appearance & Branding</h3>
                </div>
                <div class="card-body">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="primary_color">Primary Color</label>
                            <input type="color" id="primary_color" name="primary_color" class="color-input"
                                   value="<%= clubSettings ? clubSettings.primary_color || '#667eea' : '#667eea' %>">
                            <small class="help-text">Main brand color for headers and buttons</small>
                        </div>

                        <div class="form-group">
                            <label for="secondary_color">Secondary Color</label>
                            <input type="color" id="secondary_color" name="secondary_color" class="color-input"
                                   value="<%= clubSettings ? clubSettings.secondary_color || '#764ba2' : '#764ba2' %>">
                            <small class="help-text">Accent color for highlights and links</small>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h3><i class="fas fa-globe"></i> Regional Settings</h3>
                </div>
                <div class="card-body">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="locale">Language/Locale</label>
                            <select id="locale" name="locale">
                                <option value="en-US" <%= clubSettings && clubSettings.locale === 'en-US' ? 'selected' : '' %>>English (US)</option>
                                <option value="en-GB" <%= clubSettings && clubSettings.locale === 'en-GB' ? 'selected' : '' %>>English (UK)</option>
                                <option value="es-ES" <%= clubSettings && clubSettings.locale === 'es-ES' ? 'selected' : '' %>>Spanish</option>
                                <option value="fr-FR" <%= clubSettings && clubSettings.locale === 'fr-FR' ? 'selected' : '' %>>French</option>
                                <option value="de-DE" <%= clubSettings && clubSettings.locale === 'de-DE' ? 'selected' : '' %>>German</option>
                                <option value="pt-BR" <%= clubSettings && clubSettings.locale === 'pt-BR' ? 'selected' : '' %>>Portuguese (Brazil)</option>
                            </select>
                            <small class="help-text">Default language for date formats and system messages</small>
                        </div>

                        <div class="form-group">
                            <label for="timezone">Default Timezone</label>
                            <select id="timezone" name="timezone">
                                <option value="America/New_York" <%= clubSettings && clubSettings.timezone === 'America/New_York' ? 'selected' : '' %>>Eastern Time (America/New_York)</option>
                                <option value="America/Chicago" <%= clubSettings && clubSettings.timezone === 'America/Chicago' ? 'selected' : '' %>>Central Time (America/Chicago)</option>
                                <option value="America/Denver" <%= clubSettings && clubSettings.timezone === 'America/Denver' ? 'selected' : '' %>>Mountain Time (America/Denver)</option>
                                <option value="America/Los_Angeles" <%= clubSettings && clubSettings.timezone === 'America/Los_Angeles' ? 'selected' : '' %>>Pacific Time (America/Los_Angeles)</option>
                                <option value="America/Phoenix" <%= clubSettings && clubSettings.timezone === 'America/Phoenix' ? 'selected' : '' %>>Arizona Time (America/Phoenix)</option>
                                <option value="America/Anchorage" <%= clubSettings && clubSettings.timezone === 'America/Anchorage' ? 'selected' : '' %>>Alaska Time (America/Anchorage)</option>
                                <option value="Pacific/Honolulu" <%= clubSettings && clubSettings.timezone === 'Pacific/Honolulu' ? 'selected' : '' %>>Hawaii Time (Pacific/Honolulu)</option>
                                <option value="Europe/London" <%= clubSettings && clubSettings.timezone === 'Europe/London' ? 'selected' : '' %>>London Time (Europe/London)</option>
                                <option value="Europe/Paris" <%= clubSettings && clubSettings.timezone === 'Europe/Paris' ? 'selected' : '' %>>Paris Time (Europe/Paris)</option>
                                <option value="Asia/Tokyo" <%= clubSettings && clubSettings.timezone === 'Asia/Tokyo' ? 'selected' : '' %>>Tokyo Time (Asia/Tokyo)</option>
                            </select>
                            <small class="help-text">Default timezone for your club (can be overridden per location)</small>
                        </div>
                    </div>
                </div>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i> Save Settings
                </button>
            </div>
        </form>
    </div>

    <footer class="footer">
        <div class="footer-content">
            <p>&copy; 2025 DojoPro. All rights reserved.</p>
        </div>
    </footer>
    <script src="/js/main.js"></script>
</body>
</html>
