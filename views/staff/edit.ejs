<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Staff Member - DojoPro Admin</title>
    <link href="/css/admin.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <!-- Header -->
    <div class="header">
        <div class="header-content">
            <div class="logo">🥋 DojoPro</div>
            <div class="club-info">
                <% if (userClub) { %>
                    <div class="club-name"><%= userClub.club_name %></div>
                    <div style="font-size: 0.9rem; opacity: 0.8;">Club Management Portal</div>
                <% } %>
            </div>
            <div class="user-info">
                <div><%= user.first_name %> <%= user.last_name %></div>
                <form method="POST" action="/logout" style="display: inline;">
                    <button type="submit" class="btn-logout">Logout</button>
                </form>
            </div>
        </div>
    </div>

    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-brand">
                <a href="/dashboard">DojoPro Admin</a>
            </div>
            <div class="nav-menu">
                <a href="/dashboard" class="nav-link">
                    <i class="fas fa-tachometer-alt"></i> Dashboard
                </a>
                <a href="/members" class="nav-link">
                    <i class="fas fa-users"></i> Members
                </a>
                <a href="/locations" class="nav-link">
                    <i class="fas fa-map-marker-alt"></i> Locations
                </a>
                <a href="/staff" class="nav-link active">
                    <i class="fas fa-user-tie"></i> Staff
                </a>
                <a href="/check-ins" class="nav-link">
                    <i class="fas fa-clock"></i> Check-ins
                </a>
                <a href="/settings" class="nav-link">
                    <i class="fas fa-cog"></i> Settings
                </a>
            </div>
        </div>
    </nav>

    <div class="main-content">
        <div class="content-header">
            <h1><i class="fas fa-edit"></i> Edit Staff Member</h1>
            <a href="/staff" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to Staff
            </a>
        </div>

        <% if (messages.error) { %>
            <div class="alert alert-error">
                <i class="fas fa-exclamation-circle"></i> <%= messages.error %>
            </div>
        <% } %>

        <div class="card">
            <div class="card-header">
                <h3><i class="fas fa-user-tie"></i> Edit: <%= staff.first_name %> <%= staff.last_name %></h3>
            </div>
            <div class="card-body">
                <form method="POST" action="/staff/<%= staff.user_id %>/edit">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="first_name"><i class="fas fa-user"></i> First Name *</label>
                            <input type="text" id="first_name" name="first_name" required 
                                   value="<%= staff.first_name %>">
                        </div>
                        <div class="form-group">
                            <label for="last_name"><i class="fas fa-user"></i> Last Name *</label>
                            <input type="text" id="last_name" name="last_name" required 
                                   value="<%= staff.last_name %>">
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="email"><i class="fas fa-envelope"></i> Email Address</label>
                        <input type="email" id="email" name="email" readonly class="readonly-field"
                               value="<%= staff.email %>">
                        <small class="help-text">Email address cannot be changed</small>
                    </div>

                    <div class="form-group">
                        <label for="phone"><i class="fas fa-phone"></i> Phone Number</label>
                        <input type="tel" id="phone" name="phone" 
                               value="<%= staff.phone || '' %>"
                               placeholder="Enter phone number">
                    </div>

                    <div class="form-group">
                        <label for="role"><i class="fas fa-user-tag"></i> Role *</label>
                        <select id="role" name="role" required>
                            <option value="admin" <%= staff.role === 'admin' ? 'selected' : '' %>>Admin</option>
                            <option value="instructor" <%= staff.role === 'instructor' ? 'selected' : '' %>>Instructor</option>
                            <option value="staff" <%= staff.role === 'staff' ? 'selected' : '' %>>Staff</option>
                        </select>
                        <small class="help-text">
                            <strong>Admin:</strong> Full management access | 
                            <strong>Instructor:</strong> Classes and members | 
                            <strong>Staff:</strong> Basic operations
                        </small>
                    </div>

                    <div class="form-group">
                        <label>
                            <input type="checkbox" name="is_primary_contact" value="1" 
                                   <%= staff.is_primary_contact ? 'checked' : '' %>>
                            <i class="fas fa-star"></i> Make this the primary contact
                        </label>
                        <small class="help-text">Primary contact will receive important club notifications</small>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> Update Staff Member
                        </button>
                        <a href="/staff" class="btn btn-secondary">
                            <i class="fas fa-times"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <footer class="footer">
        <div class="footer-content">
            <p>&copy; 2025 DojoPro. All rights reserved.</p>
        </div>
    </footer>
    <script src="/js/main.js"></script>
</body>
</html>
