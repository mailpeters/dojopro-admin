<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .auth-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
            width: 100%;
            max-width: 500px;
        }

        .auth-header {
            background: linear-gradient(45deg, #ff6b6b, #ff8e8e);
            color: white;
            padding: 40px;
            text-align: center;
        }

        .auth-header h1 {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .auth-header p {
            opacity: 0.9;
            font-size: 1.1rem;
        }

        .auth-content {
            padding: 40px;
            text-align: center;
        }

        .setup-icon {
            font-size: 4rem;
            margin-bottom: 20px;
            color: #667eea;
        }

        .setup-message {
            color: #333;
            margin-bottom: 30px;
            line-height: 1.6;
        }

        .setup-message h2 {
            color: #667eea;
            margin-bottom: 15px;
        }

        .contact-info {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .contact-info h3 {
            color: #333;
            margin-bottom: 10px;
        }

        .btn {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 10px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.3s ease;
            text-decoration: none;
            display: inline-block;
            margin: 10px;
        }

        .btn:hover {
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: #6c757d;
        }

        .alert {
            padding: 12px 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-weight: 500;
        }

        .alert-success {
            background: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
        }

        .alert-error {
            background: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
        }

        .alert-info {
            background: #d1ecf1;
            border: 1px solid #bee5eb;
            color: #0c5460;
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <div class="auth-header">
            <h1>📊 ClubDirector</h1>
            <p>Account Setup Required</p>
        </div>
        
        <div class="auth-content">
            <!-- Flash Messages -->
            <% if (messages.success && messages.success.length > 0) { %>
                <% messages.success.forEach(function(message) { %>
                    <div class="alert alert-success"><%= message %></div>
                <% }); %>
            <% } %>
            
            <% if (messages.error && messages.error.length > 0) { %>
                <% messages.error.forEach(function(message) { %>
                    <div class="alert alert-error"><%= message %></div>
                <% }); %>
            <% } %>
            
            <% if (messages.info && messages.info.length > 0) { %>
                <% messages.info.forEach(function(message) { %>
                    <div class="alert alert-info"><%= message %></div>
                <% }); %>
            <% } %>

            <div class="setup-icon">⚙️</div>
            
            <div class="setup-message">
                <h2>Account Setup Required</h2>
                <p>Your account has been created, but you don't have access to any clubs yet. Please contact your system administrator to:</p>
                
                <div class="contact-info">
                    <h3>Next Steps:</h3>
                    <ul style="list-style: none; padding: 0;">
                        <li>✓ Request access to a club</li>
                        <li>✓ Get assigned an appropriate role</li>
                        <li>✓ Receive proper permissions</li>
                    </ul>
                </div>
                
                <p>Once your access has been configured, you'll be able to log in and manage your club(s).</p>
            </div>

            <div>
                <a href="/logout" class="btn btn-secondary">Logout</a>
                <a href="/dashboard" class="btn">Try Again</a>
            </div>
        </div>
    </div>
</body>
</html>